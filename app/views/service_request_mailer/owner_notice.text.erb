You have created service <%= 'request'.pluralize(@count) %> on Project Jellyfish for <%= @project.name %>.

<%= 'It'.pluralize(@count) %> is pending review. We will notify you once <%= 'it'.pluralize(@count) %> <%= 'is'.pluralize(@count) %> approved or denied.

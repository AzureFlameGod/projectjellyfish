(function () {
  'use strict';

  angular.module('components.auth')
    .controller('RemoteLoginController', Controller);

  /** @ngInject */
  function Controller(AppSettings) {
    var ctrl = this;

    // add handlers as needed.
  }
})();
